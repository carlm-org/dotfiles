- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    # fish
    ~/.config/fish/config.fish: fish/config.fish

    # git
    ~/.gitconfig: git/gitconfig

    # vim
    ~/.vimrc: vim/vimrc
