- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    # git
    ~/.gitconfig: git/gitconfig

    # vim
    ~/.vimrc: vim/vimrc
    ~/.vim/bundle/Vundle.vim: vim/Vundle.vim
