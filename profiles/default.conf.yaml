- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    # git
    ~/.gitconfig: git/gitconfig

    # vim
    ~/.vim/bundle: vim/bundle

    # tmux
    ~/.tmux.conf: tmux/tmux.conf
